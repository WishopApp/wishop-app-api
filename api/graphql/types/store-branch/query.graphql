type Query {
  storeBranch (
    _id: ID
    beaconToken: ID
  ): StoreBranch

  storeBranches (
    limit: Int
    skip: Int
  ): [StoreBranch]

  searchStoreBranch (
    beaconToken: String
  ): StoreBranch
}
