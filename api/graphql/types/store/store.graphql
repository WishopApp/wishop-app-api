type Store {
  _id: ID
  ownerId: ID
  name: String
  coverUrl: String
  avatarUrl: String
  description: String
  owner: User
  beacons: [Beacon]
  createdAt: String
}

type StoreStatistic {
  open: Int
  closed: Int
  banned: Int
  total: Int
}
