type Mutation {
  createStore(
    ownerId: ID!
    name: String!
    coverUrl: String
    avatarUrl: String
    description: String
  ): Store

  updateStore(
    _id: ID!
    name: String
    coverUrl: String
    avatarUrl: String
    description: String
    promotions: [String]
  ): Store
}
