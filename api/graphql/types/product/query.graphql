type Query {
  product (
    _id: ID
    name: String!
  ): Product

  products (
    limit: Int
    skip: Int
  ): [Product]
  
  searchByKeyword (
    keyword: String!
  ): [Product]

  searchByWishlist (
    wishlist: WishlistInput!
  ): [ProductFromSearch]
}
