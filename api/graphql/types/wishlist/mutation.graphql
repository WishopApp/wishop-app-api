type Mutation {
  createWishlist (
    userId: ID!
    wishlist: WishlistInput!
  ): User

  updateWishlist (
    userId: ID!
    wishlistId: ID!
    wishlist: UpdateWishlist!
  ): User

  removeWishlist (
    userId: ID!
    wishlistId: ID!
  ): User
}
