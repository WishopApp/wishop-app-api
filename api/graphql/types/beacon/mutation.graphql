type Mutation {
  createBeacon(
    name: String!
    type: BEACON_TYPES
    status: BEACON_STATUSES
    locationX: Int
    locationY: Int
    identifier: String
    uuid: String!
    major: Int!
    minot: Int!
    assignId: ID
  ): Beacon
}
