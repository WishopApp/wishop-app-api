enum BEACON_STATUSES {
  IDLE
  INUSE
  EXPIRE
}

enum BEACON_TYPES {
  INDOOR
  STICKER
}

type Beacon {
  _id: ID
  name: String
  type: BEACON_TYPES
  status: BEACON_STATUSES
  locationX: Int
  locationY: Int
  identifier: String
  uuid: String
  major: Int
  minot: Int
  assignId: ID
}
